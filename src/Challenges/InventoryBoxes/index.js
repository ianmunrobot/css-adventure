import Challenge from './Challenge'
import tests from './tests'

export default {
  challengeComponent: Challenge,
  prompt: '...oh no! The hacker has destroyed the formatting of my inventory!\nThe inventory should display as some sort of grid, with three rows, no bullet points (!?), a black background, and a light gray (#c6c6c6) border between items',
  tests,
}